{
  "name": "zingcab-backend",
  "version": "1.0.0",
  "description": "ZingCab Backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "cross-env NODE_ENV=test jest --testTimeout=10000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.0.2",
    "nodemon": "^3.0.2",
    "supertest": "^7.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
